import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply Online 2027 | Private School Eastern Cape",
  description:
    "Apply online to Stutterheim International School for the 2027 academic year. Simple 3-step process — limited spaces available. Grade R to Grade 9.",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
